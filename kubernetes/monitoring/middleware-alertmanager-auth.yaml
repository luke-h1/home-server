# Alertmanager ingress basic auth (secret: k8s.sh secrets). Traefik 3 / k3s.
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: alertmanager-auth
  namespace: monitoring
spec:
  basicAuth:
    secret: alertmanager-basic-auth
